{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||'';if(!ua.includes('pnpm')){console.error('Use pnpm instead');process.exit(1)}\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/nexus run dev",
    "db:generate": "pnpm --filter @workspace/db run generate",
    "db:migrate": "node ./scripts/apply-migrations.mjs",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:setup": "node ./scripts/apply-migrations.mjs && pnpm run db:seed",
    "db:seed": "pnpm --filter @workspace/scripts run seed"
  },
  "private": true,
  "devDependencies": {
    "dotenv": "^16.4.7",
    "mysql2": "^3.14.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
