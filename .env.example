# MySQL (WAMP) — adjust user/password/host as needed
DATABASE_URL=mysql://root@127.0.0.1:3307/chatex

# API server
PORT=5000
SESSION_SECRET=change-me-in-production
NODE_ENV=development
LOG_LEVEL=info

# Frontend (Vite)
VITE_PORT=5173
BASE_PATH=/
